# Phase 9 Capital correctness review

Result: pass, subject to final full regression.

All six signals are documented and derived through one Analytics-owned feature provider. Base weights total 100. Optional customer quality is explicitly not applicable and proportionally reweighted; required missing evidence yields `insufficient_data`. Pure calculation is reproducible for organization, environment, date, currency, and `rules-1`. Immutable snapshots preserve configuration meaning, aggregate evidence, contributions, explanations, and deterministic deltas. GMV/order/transaction variants are not separately rewarded.
